# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# Option 1: Read secrets from the environment
# KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

# Option 2: Read secrets via a command
# RAILS_MASTER_KEY=$(cat config/master.key)

# Option 3: Read secrets via kamal secrets helpers
# These will handle logging in and fetching the secrets in as few calls as possible
# There are adapters for 1Password, LastPass + Bitwarden
#
# SECRETS=$(kamal secrets fetch --adapter 1password --account my-account --from MyVault/MyItem KAMAL_REGISTRY_PASSWORD RAILS_MASTER_KEY)
# KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD $SECRETS)
# RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY $SECRETS)

SECRETS=$(kamal secrets fetch --adapter 1password --account Neuzig --from Private/Split KAMAL_REGISTRY_PASSWORD)
KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD ${SECRETS})

AUTH_SECRETS=$(kamal secrets fetch --adapter 1password --account Neuzig --from 9to5/Auth CLIENT_ID CLIENT_SECRET)
CLIENT_ID=$(kamal secrets extract CLIENT_ID ${AUTH_SECRETS})
CLIENT_SECRET=$(kamal secrets extract CLIENT_SECRET ${AUTH_SECRETS})
IDP_DOMAIN=ninetofive.eu.auth0.com

COOKIE_KEYS=$(kamal secrets fetch --adapter 1password --account Neuzig --from 9to5/'Secure Cookie Keys' Production/'Cookie Key' Production/'Session Database Key')
COOKIE_KEY=$(kamal secrets extract Production/'Cookie Key' ${COOKIE_KEYS})
SESSION_DATABASE_KEY=$(kamal secrets extract Production/'Session Database Key' ${COOKIE_KEYS})

OAUTH_AUDIENCE=https://api.ntf.io

DB_PASSWORDS=$(kamal secrets fetch --adapter 1password --account Neuzig --from 9to5/DB DB_PASSWORD)
POSTGRES_PASSWORD=$(kamal secrets extract DB_PASSWORD ${DB_PASSWORDS})
DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@nine-to-five-db:5432/postgres

DB_KEYS=$(kamal secrets fetch --adapter 1password --account Neuzig --from 9to5/'Database Keys' Production/'database key' Production/'hash key')
DATABASE_KEY=$(kamal secrets extract Production/'database key' ${DB_KEYS})
DATABASE_HASH_KEY=$(kamal secrets extract Production/'hash key' ${DB_KEYS})
